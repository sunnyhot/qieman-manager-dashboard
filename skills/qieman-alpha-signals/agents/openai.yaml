interface:
  display_name: "Qieman Alpha Signals"
  short_description: "Native Swift commands for Qieman data, valuation, and signals"
  default_prompt: "Use $qieman-alpha-signals and its native macOS Swift CLI for incremental watch alerts, auth status, context lists, speech feeds, comments, platform actions, holdings, timelines, valuations, and JSON signal extraction."
